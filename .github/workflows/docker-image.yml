# name: Docker Image CI

# on:
#   push:
#     branches: ["main"]
#   pull_request:
#     branches: ["main"]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Cache Docker layers
#         uses: actions/cache@v3
#         with:
#           path: /tmp/.buildx-cache
#           key: ${{ runner.os }}-buildx-${{ github.sha }}
#           restore-keys: |
#             ${{ runner.os }}-buildx-

#       - name: Build the Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           file: Dockerfile
#           push: false
#           load: true
#           tags: finance-api:latest
#           cache-from: type=local,src=/tmp/.buildx-cache
#           cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

#       # Temporary fix to move cache
#       - name: Move cache
#         run: |
#           rm -rf /tmp/.buildx-cache
#           mv /tmp/.buildx-cache-new /tmp/.buildx-cache

#       - name: Run Tests
#         # We run tests inside the container to ensure the image works.
#         # We override CMD to run tests.
#         # Note: This might require a DB if tests hit the DB.
#         # For a simple self-contained test or unit tests:
#         run: |
#           docker run --rm -e DEBUG=True -e SECRET_KEY=build-secret -e USE_DOCKER=False finance-api:latest python manage.py test --noinput
